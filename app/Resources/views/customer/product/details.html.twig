{% extends ':customer:base.html.twig' %}

{% block body %}
    <div class="row">
        <div class="col-lg-12">
            <h1>{{ product.metadata.name }}</h1>
        </div>
    </div>
    <div class="row">
        <div class="col-lg-4">
            <h4>Price: {{ product.price|number_format(2, '.', ',') }} {{ product.currency }}</h4>
        </div>
    </div>
{% endblock %}
